import 'package:flutter/material.dart';

class RepresentativesPage extends StatelessWidget {
  static const routeName = '/extractArgumentsRepresentativesPage';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
